package com.hetufei.leetcode.lagou.binarySearch;

/**
 * @Author: {USER}
 * @Date: {DATE} TIME}
 * @Description: TODO
 **/
public class MaxAvgSubArr {

    public float getMaxAvgSubArr(int[] nums, int k) {
        if (nums == null || nums.length == 0 || nums.length < k) {
            return 0;
        }
        int start = 0,end = 0;
        int n = nums.length;
        float avg = 0.0f;
        int sum = 0;
        while (end < n) {
            sum += nums[end];
            while ()
        }
    }
}
