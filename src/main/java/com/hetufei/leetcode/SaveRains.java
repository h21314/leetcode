package com.hetufei.leetcode;

/**
 * @Author: {USER}
 * @Date: {DATE} TIME}
 * @Description: TODO
 **/
public class SaveRains {

    public int getRains(int[] array) {
        if (array == null || array.length == 0) {
            return 0;
        }
        int len = array.length;
        int[] dp = new int[len + 1];
        
        return 0;
    }
}
